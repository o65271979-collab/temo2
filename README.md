# موقع Temo - نظام إدارة المطعم

## نظرة عامة
موقع Temo هو نظام إدارة مطعم ذكي مبني بـ HTML/CSS/JavaScript ومتصل بـ Firebase للمصادقة وقاعدة البيانات.

## الميزات الرئيسية

### 🔐 نظام المصادقة
- تسجيل دخول آمن باستخدام Firebase Authentication
- إنشاء حسابات جديدة
- إعادة تعيين كلمة المرور
- حماية الصفحات من الوصول غير المصرح

### 👤 إدارة المستخدمين
- إضافة مستخدمين جدد
- تحديد أدوار المستخدمين (مدير، مدير فرع، موظف)
- حذف المستخدمين
- عرض قائمة المستخدمين المسجلين

### ⚙️ إعدادات الحساب
- تحديث البريد الإلكتروني
- تغيير كلمة المرور
- عرض معلومات الحساب
- إدارة المستخدمين (للمديرين)

### 📊 عرض البيانات
- عرض إحصائيات النظام
- عدد الشيفتات المسجلة
- عدد التقارير
- عدد المصاريف

## هيكل الملفات

```
temo-website/
├── index.html          # الصفحة الرئيسية
├── login.html          # صفحة تسجيل الدخول
├── settings.html       # صفحة الإعدادات
├── styles.css          # ملف التصميم الرئيسي
├── firebase-config.js  # إعدادات Firebase
├── app.js             # JavaScript للصفحة الرئيسية
├── auth.js            # JavaScript لتسجيل الدخول
├── settings.js        # JavaScript لصفحة الإعدادات
└── README.md          # هذا الملف
```

## كيفية الاستخدام

### 1. تشغيل الموقع
- افتح ملف `index.html` في المتصفح
- أو استخدم خادم محلي مثل Live Server في VS Code

### 2. إنشاء حساب جديد
1. اذهب إلى صفحة تسجيل الدخول
2. اضغط على "إنشاء حساب جديد"
3. أدخل البريد الإلكتروني وكلمة المرور
4. اضغط "إنشاء الحساب"

### 3. تسجيل الدخول
1. أدخل البريد الإلكتروني وكلمة المرور
2. اضغط "تسجيل الدخول"

### 4. إدارة الإعدادات
1. بعد تسجيل الدخول، اذهب إلى صفحة الإعدادات
2. يمكنك:
   - تحديث معلومات الحساب
   - تغيير كلمة المرور
   - إضافة مستخدمين جدد
   - إدارة المستخدمين الموجودين

## إعدادات Firebase

الموقع متصل بمشروع Firebase التالي:
- **Project ID**: temo-a8e65
- **Auth Domain**: temo-a8e65.firebaseapp.com

### المجموعات في Firestore:
- `users`: معلومات المستخدمين وأدوارهم
- `shifts`: بيانات الشيفتات
- `reports`: تقارير المبيعات
- `expenses`: المصاريف

## المتطلبات التقنية

### المتصفحات المدعومة:
- Chrome (الأحدث)
- Firefox (الأحدث)
- Safari (الأحدث)
- Edge (الأحدث)

### الاتصال بالإنترنت:
- مطلوب للاتصال بـ Firebase
- يعمل الموقع محلياً مع الاتصال بالإنترنت

## الأمان

### ميزات الأمان المطبقة:
- مصادقة Firebase الآمنة
- إعادة المصادقة قبل تحديث البيانات الحساسة
- حماية الصفحات من الوصول غير المصرح
- تشفير كلمات المرور تلقائياً بواسطة Firebase

### قواعد الأمان:
- كلمة المرور يجب أن تكون 6 أحرف على الأقل
- إعادة المصادقة مطلوبة لتغيير البريد الإلكتروني أو كلمة المرور
- التحقق من صحة البيانات قبل الإرسال

## استكشاف الأخطاء

### مشاكل شائعة وحلولها:

1. **خطأ في الاتصال بـ Firebase**
   - تأكد من الاتصال بالإنترنت
   - تحقق من إعدادات Firebase في `firebase-config.js`

2. **لا يمكن تسجيل الدخول**
   - تأكد من صحة البريد الإلكتروني وكلمة المرور
   - تحقق من تفعيل الحساب

3. **لا تظهر البيانات**
   - تأكد من وجود البيانات في Firestore
   - تحقق من أذونات قاعدة البيانات

## التطوير المستقبلي

### ميزات مقترحة:
- إضافة لوحة تحكم متقدمة
- تقارير مفصلة أكثر
- إشعارات فورية
- تطبيق موبايل
- نسخ احتياطية تلقائية

## الدعم

للحصول على المساعدة أو الإبلاغ عن مشاكل:
- راجع هذا الملف أولاً
- تحقق من وحدة التحكم في المتصفح للأخطاء
- تأكد من إعدادات Firebase

---

**تم تطوير الموقع بواسطة**: Cascade AI Assistant  
**تاريخ الإنشاء**: نوفمبر 2024  
**الإصدار**: 1.0.0
